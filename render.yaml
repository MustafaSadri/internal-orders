services:
  - type: cron
    name: daily-internal-order
    runtime: node
    plan: starter
    schedule: "0 14 * * *" # 17:00 Moscow time (UTC+3, no DST)
    buildCommand: npm ci
    startCommand: node scripts/create-internal-order.js
    envVars:
      - key: TOKEN
        sync: false
